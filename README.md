Sources for the plugin `plugin.etransition`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.etransition" = {
            publisherId = "io.joehinkle",
        },
    },
}
```
